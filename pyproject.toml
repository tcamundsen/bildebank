[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "bildebank"
version = "0.9.0"
description = "Konservativt bildesorteringsprogram for å samle bilder og videoer."
requires-python = ">=3.13,<3.14"
dependencies = [
    "h3",
    "numpy",
    "Pillow",
]

[project.optional-dependencies]
dev = [
    "mcp>=2,<3",
    "mypy",
    "pyflakes",
    "pytest",
    "ruff",
]
face = [
    "insightface==1.0.1",
    "onnxruntime",
]
openclip = [
    "igraph==1.0.0",
    "open_clip_torch",
    "scikit-learn==1.9.0",
]

[project.scripts]
bildebank = "bildebank.cli:main"

[tool.setuptools.packages.find]
include = ["bildebank*"]

[tool.setuptools.package-data]
bildebank = ["assets/*.css", "assets/*.js", "assets/icons/*.png"]

[[tool.mypy.overrides]]
module = ["h3", "igraph", "insightface.*", "open_clip", "sklearn.*", "torch"]
ignore_missing_imports = true
